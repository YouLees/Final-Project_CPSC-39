package finalProject;

import java.util.ArrayList;

public class NPCGamePlays {
	
	//gets the size of the User's play
		public static int getPlaySize(ArrayList<Card> playerHand) {
			return playerHand.size();
			
		}
		
		//checking for the length of playable card(s) 
		public static void compareSize(int size, ArrayList<Card> playerHand) {
			
			
			//base case
			if (size == 1) {
				
				//checks for playable card
				//NEEDS CODE HERE!!!!!!!!
			}
			
			if (size == 2) {
				
				//checks if it's a pair
				//NEEDS CODE HERE!!!!!!!!
				
				
			}
			
			if (size == 3) {
				
				//checks if it's 3 of a kind
				//NEEDS CODE HERE!!!!!!!!
				
				
				//checks if it's a sequence
				//NEEDS CODE HERE!!!!!!!!
				
			}
			
			if (playerHand.size() == 4) {
				//checks if it's
			}
		}
		
		//Check if the play has a sequence of cards
		public static void hasSeq() {
			
		}
		
		//Checks if the play has # of a kind
		public static void hasOfAKind() {
			
		}
		
		// checks for a bomb
		public static void hasBomb() {
			
		}

}
